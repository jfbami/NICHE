
  # Location Sharing App (Copy)

  This is a code bundle for Location Sharing App (Copy). The original project is available at https://www.figma.com/design/V8XAOMKhESsygmUfD2QgQf/Location-Sharing-App--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  